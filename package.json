{
  "author": "",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.26.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "license": "ISC",
  "main": "dist/index.js",
  "name": "dive",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=cjs --outfile=dist/index.js --sourcemap && cp LICENSE dist/",
    "run": "npm run build && node dist/index.js"
  },
  "version": "0.2.1"
}
